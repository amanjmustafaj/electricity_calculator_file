# چارەسەری کێشە | Error Fix Guide

## کێشەکە | The Problem

کاتێک هەوڵی ڕانکردنی ئەپەکە دەدەیت، ئەم هەڵەیەت بۆ دێت:

```
Unresolved reference 'ShareSuccessManager'
BUILD FAILED
```

## هۆکار | Cause

کێشەکە لە وەشانی `share_plus:10.1.4` دایە کە لەگەڵ وەشانی Kotlin/Gradle ـی تۆ هاوتا نییە.

---

## چارەسەر - ڕێگای ١ (ئاسانترین) ✅

### هەنگاو ١: pubspec.yaml بگۆڕە

فایلی `pubspec.yaml` بکەرەوە و ئەم گۆڕانکاریانە بکە:

**پێش (هەڵە):**
```yaml
dependencies:
  share_plus: ^10.0.3
  shared_preferences: ^2.3.4
  home_widget: ^0.8.0
```

**دوای (چاککراو):**
```yaml
dependencies:
  share_plus: ^7.2.2
  shared_preferences: ^2.2.3
  home_widget: ^0.6.0
```

### هەنگاو ٢: پاککردنەوە و دامەزراندنەوە

```bash
flutter clean
flutter pub get
```

### هەنگاو ٣: ڕانکردن

```bash
flutter run
```

---

## چارەسەر - ڕێگای ٢ (ئەگەر ڕێگای ١ کارنەکرد)

### گۆڕینی وەشانی Gradle

فایلی `android/build.gradle` بکەرەوە و ئەمانە نوێ بکەرەوە:

```gradle
buildscript {
    ext.kotlin_version = '1.9.0'  // ئەمە زیاد بکە
    
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'  // نوێ بکەرەوە
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}
```

دواتر:
```bash
flutter clean
flutter pub get
flutter run
```

---

## چارەسەر - ڕێگای ٣ (ئەگەر هەردوو ڕێگەکەی پێشوو کارنەکرد)

### لابردنی تایبەتمەندی Share

ئەگەر هەرکێشەیەکت مایەوە، دەتوانیت تایبەتمەندی شەریککردن لاببەیت:

١. لە `pubspec.yaml` ئەم هێڵە بسڕەوە:
```yaml
share_plus: ^7.2.2  # ئەمە بسڕەوە
```

٢. لە `lib/main.dart` ئەم import ـە بسڕەوە:
```dart
import 'package:share_plus/share_plus.dart';  // بسڕەوە
```

٣. لە کۆدەکە، هەموو `Share.share(...)` بسڕەوە یان کۆمێنت بکە.

دواتر:
```bash
flutter clean
flutter pub get
flutter run
```

---

## تێبینی گرنگ ⚠️

**هەموو تایبەتمەندییەکانی تر بە باشی کاردەکەن:**
- ✅ هەڵگرتن و گەڕاندنەوەی داتا
- ✅ هەژمارکردنی نرخ
- ✅ هەژمارە تەکنیکییەکان
- ✅ تەمی تاریک/ڕووناک
- ✅ زمانەکان (کوردی، ئینگلیزی، عەرەبی)

**تەنیا تایبەتمەندی شەریککردن:**
- ❌ لە وەشانەکانی نوێی share_plus کێشەی هەیە
- ✅ لە وەشانی ٧.٢.٢ بە باشی کاردەکات
- ✅ یان دەتوانیت بە تەواوی لایببەیت

---

## وەشانە پێشنیارکراوەکان

```yaml
dependencies:
  flutter:
    sdk: flutter
  
  # وەشانە stable ـەکان
  share_plus: ^7.2.2           # ✅ کاردەکات
  shared_preferences: ^2.2.3   # ✅ کاردەکات
  home_widget: ^0.6.0          # ✅ کاردەکات (بێ widget)
  
  # یان بێ share_plus
  shared_preferences: ^2.2.3   # ✅ هەڵگرتنی داتا کاردەکات
```

---

## بۆچی ئەم کێشەیە؟

کێشەکە لە `share_plus:10.1.4` دایە چونکە:
- فایلەکانی `ShareSuccessManager` و `SharePlusPendingIntent` نییە
- لەگەڵ وەشانە کۆنەکانی Gradle/Kotlin کێشەی هەیە
- API ـەکەی گۆڕاوە بەڵام دۆکیومێنتەیشنەکەی نوێ نەکراوەتەوە

---

## پشتیوانی زیاتر

ئەگەر هەر کێشەیەکت مایەوە:

١. **بسڕەوە و دووبارە دامەزرێنە:**
```bash
flutter clean
rm -rf build/
rm pubspec.lock
flutter pub get
```

٢. **Java JDK بپشکنە:**
- پێویستە JDK 11 یان 17
- لە Android Studio → File → Project Structure → JDK

٣. **Gradle بپشکنە:**
```bash
cd android
./gradlew clean
cd ..
flutter run
```

---

## کورتکردنەوە

**ئاسانترین ڕێگە:**
```bash
# ١. pubspec.yaml بگۆڕە بۆ share_plus: ^7.2.2
# ٢. ئەم فەرمانانە ڕان بکە:
flutter clean
flutter pub get
flutter run
```

**ئەگەر کارنەکرد:**
- وەشانی Gradle نوێ بکەرەوە (ڕێگای ٢)
- یان share_plus لابدە (ڕێگای ٣)

---

چاکەت لێبێت! 🎉

ئەگەر هەر کێشەیەکت هەبوو، زانیاریم پێبدە.
